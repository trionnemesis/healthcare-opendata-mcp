# 內部存取入口;對外暴露建議走 Internal LB / IAP,MCP server 本身無認證
apiVersion: v1
kind: Service
metadata:
  name: hcmcp
  labels:
    app: hcmcp
spec:
  type: ClusterIP
  selector:
    app: hcmcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
