{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trionnemesis.github.io/healthcare-opendata-mcp/data/schema-v1.json",
  "title": "Healthcare OpenData MCP dashboard snapshot v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "generated_at",
    "status",
    "datasets",
    "summary",
    "filters",
    "export",
    "rows"
  ],
  "properties": {
    "schema_version": { "const": "1.0" },
    "generated_at": { "type": "string", "format": "date-time" },
    "status": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "source_max_date", "message"],
      "properties": {
        "state": { "enum": ["fresh", "stale", "degraded", "empty"] },
        "source_max_date": {
          "anyOf": [
            { "type": "string", "format": "date" },
            { "type": "null" }
          ]
        },
        "message": { "type": "string" }
      }
    },
    "datasets": {
      "type": "object",
      "description": "由 catalog(已啟用者)與資料庫產生,每個資料集一筆。新增資料集不需改本 schema。",
      "required": ["pcc_tender"],
      "propertyNames": { "pattern": "^[a-z0-9_]+$" },
      "additionalProperties": { "$ref": "#/$defs/dataset" }
    },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pcc_tender"],
      "properties": {
        "pcc_tender": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "snapshot_row_count",
            "date_range",
            "announcement_types",
            "budget",
            "award_amount"
          ],
          "properties": {
            "snapshot_row_count": { "type": "integer", "minimum": 0 },
            "date_range": {
              "type": "object",
              "additionalProperties": false,
              "required": ["min", "max"],
              "properties": {
                "min": { "$ref": "#/$defs/nullableDate" },
                "max": { "$ref": "#/$defs/nullableDate" }
              }
            },
            "announcement_types": {
              "type": "array",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["name", "count"],
                "properties": {
                  "name": { "type": "string" },
                  "count": { "type": "integer", "minimum": 0 }
                }
              }
            },
            "budget": { "$ref": "#/$defs/amountSummary" },
            "award_amount": { "$ref": "#/$defs/amountSummary" }
          }
        }
      }
    },
    "filters": {
      "type": "object",
      "additionalProperties": false,
      "required": ["announcement_types", "agencies"],
      "properties": {
        "announcement_types": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        },
        "agencies": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        }
      }
    },
    "export": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "strategy",
        "max_bytes",
        "detail_limit",
        "full_row_count",
        "full_payload_bytes",
        "published_payload_bytes"
      ],
      "properties": {
        "strategy": { "enum": ["all", "latest_records"] },
        "max_bytes": { "type": "integer", "minimum": 1 },
        "detail_limit": { "type": "integer", "minimum": 1 },
        "full_row_count": { "type": "integer", "minimum": 0 },
        "full_payload_bytes": { "type": "integer", "minimum": 0 },
        "published_payload_bytes": { "type": "integer", "minimum": 0 }
      }
    },
    "rows": {
      "type": "array",
      "items": { "$ref": "#/$defs/pccRow" }
    }
  },
  "$defs": {
    "nullableString": {
      "anyOf": [{ "type": "string" }, { "type": "null" }]
    },
    "nullableDate": {
      "anyOf": [
        { "type": "string", "format": "date" },
        { "type": "null" }
      ]
    },
    "nullableNumber": {
      "anyOf": [{ "type": "number" }, { "type": "null" }]
    },
    "nullableInteger": {
      "anyOf": [{ "type": "integer", "minimum": 0 }, { "type": "null" }]
    },
    "nullableUri": {
      "anyOf": [{ "type": "string", "format": "uri" }, { "type": "null" }]
    },
    "dataset": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "row_count",
        "last_fetched_at",
        "source_url",
        "license"
      ],
      "properties": {
        "row_count": {
          "description": "null = 物化表不存在,即從未同步成功。與「同步成功但 0 筆」不同,不可折成 0。",
          "$ref": "#/$defs/nullableInteger"
        },
        "last_fetched_at": { "$ref": "#/$defs/nullableString" },
        "latest_run_status": { "$ref": "#/$defs/nullableString" },
        "latest_run_finished_at": { "$ref": "#/$defs/nullableString" },
        "source_url": { "$ref": "#/$defs/nullableUri" },
        "license": { "$ref": "#/$defs/nullableString" },
        "title": { "$ref": "#/$defs/nullableString" },
        "collection": { "$ref": "#/$defs/nullableString" },
        "update_cadence": { "$ref": "#/$defs/nullableString" },
        "verified_at": {
          "description": "catalog 記錄的官方端點實查日期;非 catalog 驅動的資料集為 null。",
          "$ref": "#/$defs/nullableString"
        },
        "stale_after_days": {
          "description": "過期門檻(天),由 update_cadence 推導(2 倍更新週期)。null = 無契約可依據。",
          "$ref": "#/$defs/nullableInteger"
        },
        "freshness": {
          "description": "unknown = 無更新頻率契約或尚未同步,不得被讀成「已驗證為最新」。",
          "enum": ["fresh", "stale", "unknown"]
        }
      }
    },
    "amountSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["known_count", "sum_twd"],
      "properties": {
        "known_count": { "type": "integer", "minimum": 0 },
        "sum_twd": { "$ref": "#/$defs/nullableNumber" }
      }
    },
    "pccRow": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "date",
        "announcement_type",
        "title",
        "agency",
        "job_number",
        "bid_deadline",
        "open_date",
        "budget",
        "award_price",
        "companies"
      ],
      "properties": {
        "date": { "$ref": "#/$defs/nullableDate" },
        "announcement_type": { "$ref": "#/$defs/nullableString" },
        "title": { "$ref": "#/$defs/nullableString" },
        "agency": { "$ref": "#/$defs/nullableString" },
        "job_number": { "$ref": "#/$defs/nullableString" },
        "bid_deadline": { "$ref": "#/$defs/nullableString" },
        "open_date": { "$ref": "#/$defs/nullableString" },
        "budget": { "$ref": "#/$defs/nullableNumber" },
        "award_price": { "$ref": "#/$defs/nullableNumber" },
        "companies": { "$ref": "#/$defs/nullableString" }
      }
    }
  }
}
